# requirements.txt — Dependencias de TODO scripts_ml/
# SIEM Inteligente | UTN FRM | Gianfranco Canciani
#
# Instalación:
#   pip install -r requirements.txt
#
# Desde el contenedor Docker:
#   docker exec -it siem_ml pip install -r /app/requirements.txt
#
# CORREGIDO: antes solo listaba las dependencias de motor_ml.py.
# Se agregaron psycopg2-binary, scipy y matplotlib, que usan
# medir_latencias.py, medir_grupo_manual.py, verificar_pg.py y
# analizar_experimento.py (antes había que instalarlas aparte,
# a mano, según README_experimento.md).

# Motor de Machine Learning
scikit-learn>=1.3.0

# Cliente de Elasticsearch
elasticsearch>=8.0.0,<9.0.0

# Manejo de datos y features
pandas>=2.0.0
numpy>=1.24.0

# HTTP para webhooks a n8n
requests>=2.31.0

# Scripts de medición del experimento (MTTD/MTTR) y análisis estadístico
psycopg2-binary>=2.9.0
scipy>=1.11.0
matplotlib>=3.7.0

